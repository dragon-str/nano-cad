[package]
name = "nanocad-engine"
description = "L1 atomistic engine: pair lists, forces, integrators"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
nanocad-model.workspace = true
thiserror.workspace = true

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "engine_bench"
harness = false
